// The app does not use the SwiftUI App lifecycle: Epsilon's SDL backend must
// own the main run loop, so the entry point is App/main.swift and the
// application layer is coordinated by AppController.
